.row
  .col-sm-6
    .form-group(ng-class="{ 'has-error': updateForm.hasError(entryAttrVM.form.emailAddress) }")
      label Email
      input.form-control(
        type="email"
        name="emailAddress"
        ng-model="entryAttrVM.data.medium.emailAddress"
        required
      )
      .help-block(
        ng-messages="entryAttrVM.form.emailAddress.$error"
        ng-show="updateForm.hasError(entryAttrVM.form.emailAddress)"
      )
        span(ng-message="required") This field is required.
        span(ng-message="email") This field must be a valid email.